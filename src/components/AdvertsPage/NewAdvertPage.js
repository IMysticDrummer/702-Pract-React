

import Layout from '../Layout/Layout.js';

const NewAdvertPage = ({subTitle}) => {
  return <Layout subTitle={subTitle}><span>Esto es new advert page</span></Layout>;
};

export default NewAdvertPage;
